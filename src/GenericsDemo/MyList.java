package GenericsDemo;

public class MyList<T> { // "<T>" makes it generic class. T means nothing, it can be any letter but usually we write T, means TYPE;
    public void add(T value) {

    }

    public void remove(T value) {

    }
}
